# backend/requirements.txt
# Version: 1.4
# Mô tả: Danh sách các thư viện Python, ưu tiên các phiên bản mới và ổn định.

fastapi==0.111.0
uvicorn[standard]==0.30.1 # Bao gồm websockets và uvloop
python-dotenv==1.0.1
httpx==0.27.0

# Database: SQLAlchemy ORM và driver ASYNC cho PostgreSQL
sqlalchemy==2.0.30
asyncpg==0.29.0

# Bảo mật: Băm mật khẩu và JWT
bcrypt==4.1.3
python-jose[cryptography]==3.4.0 # Đảm bảo cryptography được cài cùng
authlib==1.3.0 # Cho Google OAuth

# Xác thực token (itsdangerous) và Rate Limiting
itsdangerous==2.2.0
slowapi==0.1.9

# Pydantic
pydantic==2.7.4 
pydantic-settings==2.3.4

# Timezone handling
pytz==2024.1 # THÊM DÒNG NÀY